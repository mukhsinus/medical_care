# 🆕 ОБНОВЛЕНИЕ: IKPU коды на уровне товаров

## Что изменилось?

**Ваша система теперь поддерживает IKPU коды на уровне каждого товара**, а не просто на уровне аккаунта.

Это позволяет:
- ✅ Каждому товару иметь свой Payme Merchant ID
- ✅ Правильно маршрутизировать платежи
- ✅ Подготовиться к мультивендорной системе

## 📋 Что было добавлено

### 1. Структура данных (CatalogData.ts)

```typescript
export type CatalogItem = {
  // ... существующие поля
  
  // НОВОЕ ПОЛЕ:
  ikpuCode?: string;  // 16-digit Payme merchant ID
};
```

### 2. Валидация в Backend (paymentController.js)

```javascript
if (provider === "payme") {
  // ✅ Проверяем что каждый товар имеет IKPU
  for (const item of items) {
    if (!item.ikpuCode) {
      return error("Item missing IKPU code");
    }
  }
  
  // ✅ Проверяем что все IKPU коды одинаковые
  const ikpuCodes = [...new Set(items.map(i => i.ikpuCode))];
  if (ikpuCodes.length > 1) {
    return error("Items from different merchants");
  }
}
```

### 3. Сохранение в БД (Order.js)

```javascript
{
  // ... существующие поля
  
  // НОВОЕ ПОЛЕ:
  itemIkpuCodes: [{ type: String }]  // массив IKPU кодов товаров
}
```

### 4. Frontend примеры (PaymentIntegrationExample.tsx)

```typescript
interface CartItem {
  // ... существующие поля
  
  // НОВОЕ ПОЛЕ:
  ikpuCode?: string;  // 16-digit Payme merchant ID
}

// Проверка перед платежом
if (provider === 'payme') {
  const missingIkpu = cartItems.filter(item => !item.ikpuCode);
  if (missingIkpu.length > 0) {
    throw new Error('Some items are missing IKPU codes');
  }
}
```

## 🎯 Как использовать

### Шаг 1: Добавьте IKPU коды в каталог

```typescript
// src/data/CatalogData.ts

export const items = [
  {
    id: 1,
    category: "injection",
    nameKey: "syring",
    price: 25000,
    // ➕ ДОБАВЬТЕ:
    ikpuCode: "507144111111111",  // 16-digit Payme ID
  },
  {
    id: 2,
    category: "equipment",
    nameKey: "mask",
    price: 10000,
    // ➕ ДОБАВЬТЕ:
    ikpuCode: "507144111111111",  // ← ОДИНАКОВЫЙ IKPU
  },
];
```

### Шаг 2: Все готово!

Backend автоматически:
- ✅ Проверит что товары имеют IKPU
- ✅ Проверит что IKPU одинаковые
- ✅ Использует IKPU в платежной ссылке
- ✅ Сохранит IKPU в заказе

## 📖 Новые документы

| Файл | Содержание |
|------|-----------|
| [ITEM_IKPU_CODES.md](./ITEM_IKPU_CODES.md) | Полная информация об IKPU кодах товаров |
| [HOW_TO_ADD_IKPU_CODES.md](./HOW_TO_ADD_IKPU_CODES.md) | Пошаговая инструкция по добавлению |

## 🔄 Workflow платежа с IKPU товаров

```
1. User выбирает товары из каталога
   ├─ Товар A (ikpuCode: "507144111111111")
   ├─ Товар B (ikpuCode: "507144111111111")  ← ОДИНАКОВЫЙ
   └─ Товар C (ikpuCode: "507144111111111")  ← ОДИНАКОВЫЙ

2. Frontend отправляет заказ
   ├─ items: [
   │   {id: 1, ikpuCode: "507144111111111"},
   │   {id: 2, ikpuCode: "507144111111111"},
   │ ]
   └─ provider: "payme"

3. Backend валидирует
   ├─ ✅ Каждый товар имеет ikpuCode
   ├─ ✅ Все ikpuCode одинаковые
   └─ ✅ Может обработать платеж

4. Backend генерирует Payme URL
   └─ https://checkout.paycom.uz/507144111111111?orderId=...

5. User платит через Payme
   └─ Платеж обрабатывается для правильного IKPU

6. Заказ обновляется
   └─ itemIkpuCodes: ["507144111111111"]
```

## ✅ Проверка работоспособности

### Тест 1: Товары имеют IKPU

```bash
# Откройте DevTools (F12) → Console
# Выберите товар и проверьте:

console.log(cartItem);
// Должно быть:
// {
//   id: 1,
//   name: "Syring",
//   ikpuCode: "507144111111111"  ← ВОТ ЭТО
// }
```

### Тест 2: Платежный запрос содержит IKPU

```bash
# DevTools → Network → POST /api/payments/create
# Смотрите Payload:

{
  "items": [
    {
      "id": 1,
      "ikpuCode": "507144111111111"  ← ВОТ ЭТО
    }
  ],
  "provider": "payme"
}
```

### Тест 3: Backend используют IKPU в URL

```bash
# Response содержит:

{
  "redirectUrl": "https://checkout.paycom.uz/507144111111111?..."
                                          ^^^^^^^^^^^^^^^^
                                          IKPU из товара
}
```

## ⚠️ Ограничения

### ❌ Нельзя смешивать товары с разными IKPU

```
Ошибка:
Товар A (IKPU: 507144111111111)
Товар B (IKPU: 507144222222222)  ← РАЗНЫЙ IKPU
└─ Результат: "Items from different merchants cannot be purchased together"
```

### ✅ Правило: Все товары в заказе должны быть от одного vendora

```
OK:
Товар A (IKPU: 507144111111111)
Товар B (IKPU: 507144111111111)  ← ОДИНАКОВЫЙ
Товар C (IKPU: 507144111111111)  ← ОДИНАКОВЫЙ
└─ Результат: Платеж успешен
```

## 🔧 Технические детали

### Как IKPU код используется?

```javascript
// 1. Считываем IKPU из товаров
const itemIkpuCode = items[0].ikpuCode;  // "507144111111111"

// 2. Генерируем платежную ссылку
const redirectUrl = 
  `https://checkout.paycom.uz/${itemIkpuCode}?orderId=${orderId}&amount=${amount}`;

// 3. Сохраняем IKPU в заказе
order.itemIkpuCodes = items.map(i => i.ikpuCode);  // ["507144111111111"]

// 4. Payme отправляет callback на backend
// Callback включает order_id для поиска
```

### Структура Order в БД

```javascript
{
  _id: ObjectId(...),
  userId: ObjectId(...),
  items: [
    { id: 1, name: "Syring", ikpuCode: "507144111111111" }
  ],
  amount: 25000,
  currency: "UZS",
  paymentProvider: "payme",
  paymentStatus: "pending",
  
  // НОВОЕ ПОЛЕ:
  itemIkpuCodes: ["507144111111111"],  ← IKPU коды товаров
  
  providerTransactionId: null,
  meta: {...},
  createdAt: Date,
  updatedAt: Date
}
```

## 📚 Документация

**Главные файлы для прочтения:**

1. [HOW_TO_ADD_IKPU_CODES.md](./HOW_TO_ADD_IKPU_CODES.md) - **НАЧНИТЕ ОТСЮДА** 👈
   - Как добавить IKPU коды в каталог
   - Пошаговая инструкция
   - Примеры

2. [ITEM_IKPU_CODES.md](./ITEM_IKPU_CODES.md) - Подробная информация
   - Структура данных
   - Workflow платежа
   - Backend обработка
   - Примеры запросов

3. [PAYME_INTEGRATION.md](./PAYME_INTEGRATION.md) - Полная документация Payme
   - API детали
   - Callbacks
   - Security

## 🚀 Что дальше?

1. **Откройте** [HOW_TO_ADD_IKPU_CODES.md](./HOW_TO_ADD_IKPU_CODES.md)
2. **Добавьте** IKPU коды в [CatalogData.ts](./src/data/CatalogData.ts)
3. **Протестируйте** создание заказа
4. **Проверьте** что платеж проходит

## ❓ Часто задаваемые вопросы

**Q: Все товары должны иметь одинаковый IKPU?**
A: Да, в текущей версии. Это упростит обработку платежей.

**Q: Может ли товар не иметь IKPU?**
A: Нет. Если товар не имеет IKPU, backend вернет ошибку для Payme платежей.

**Q: Как узнать свой IKPU?**
A: На https://merchant.paycom.uz после регистрации.

**Q: Это работает с Click и Uzum?**
A: Нет, только для Payme. Click и Uzum работают с глобальным PAYME_MERCHANT_ID.

**Q: Будут ли изменения?**
A: Возможно, для поддержки мультивендорных систем потребуется разбить заказ на несколько платежей.

---

## ✨ Готово!

Ваша система теперь полностью поддерживает **IKPU коды на уровне товаров** для правильной обработки платежей через Payme! 🎉

**Следующий шаг:** [HOW_TO_ADD_IKPU_CODES.md](./HOW_TO_ADD_IKPU_CODES.md)
